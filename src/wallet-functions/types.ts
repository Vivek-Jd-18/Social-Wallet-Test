export type sendNativeType = {
    message: string;
    data: any;
    status: boolean;
}

export type estimateGasFeesType = {
    data: any;
    message: string;
    status: boolean;
}

export type transactionType = {
    data: any;
    message: string;
    status: boolean;
}
